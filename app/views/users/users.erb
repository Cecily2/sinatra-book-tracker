<% @title = "View All Users" %>

<ul>
  <% User.all.each do |u| %>
  <li><a href="/users/<%= u.id %>"><%= u.username %></a> (<%= u.books.count %> <%= u.books.count > 1 ? "books" : "book"%>)</li>
  <% end %>
</ul>